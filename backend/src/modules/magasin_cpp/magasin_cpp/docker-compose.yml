services:
  magasin_cpp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: magasin_cpp_service
    ports:
      - "3001:3001"
    networks:
      - magasin-network
    env_file:
      - .env 

networks:
  magasin-network:
    driver: bridge